<p>
  <b>Name:</b>
  <%=h @menu.name %>
</p>

<p>
  <b>Parent:</b>
  <%=h @menu.ancestors.first.name if @menu.ancestors.present? %>
</p>


<%= link_to 'Edit', edit_menu_path(@menu) %> |
<%= link_to 'Back', menus_path %>